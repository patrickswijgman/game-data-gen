export const enum Type {
  SOA = "soa",
  AOS = "aos",
  STRUCT = "struct",
  GROUP = "group",
}

export const enum FieldType {
  STRING = "string",
  NUMBER = "number",
  BOOLEAN = "boolean",
  ARRAY = "array",
}

export const enum ArrayType {
  STRING = "string",
  NUMBER = "number",
  BOOLEAN = "boolean",
}
